using System;
using System.Collections.Generic;
using System.Text;

namespace DirectAxis.RaceGame.FrontEnd.Helpers
{
    public enum RacePointTypes
    {
        Corner,
        Straight
    }

    public enum VehicleAttributes
    {
        Acceleration = 8,
        Braking = 3,
        Cornering = 4,
        TopSpeed = 9
    }
}
